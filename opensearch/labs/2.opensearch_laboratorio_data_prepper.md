# Laboratorio de Data Prepper
**Data Prepper** é unha ferramenta de inxesta integrada no ecosistema **OpenSearch**, alternativa a **Logstash** no ecosistema **ELK**. Para máis información, consultar o documento correspondente.

Neste laboratorio imos configurar un stack de docker para executar algúns exemplos básicos de inxesta de datos con **Data Prepper**. Para iso imos realizar os seguintes pasos:
1. Preparar e configurar o entorno docker.
2. Crear e validar as pipelines de Data Prepper.
3. Executar os produtores de datos.
4. Verificar a indexación en OpenSearch.
5. Visualizar os datos en OpenSearch Dashboards.
6. Realizar pequenas modificacións e probas.


## 1. Preparar e configurar o entorno docker
> Aínda que neste documento se contempla a construción paso a paso do contorno docker, está tamén dispoñible para descargar no repositorio [adbgonzalez/opensearch-stack](https://github.com/adbgonzalez/opensearch-stack)

Imos partir do `docker-compose.yml` realizado en sesións previas que contén un contedor de `opensearch-dashboards` e outro de `opensearch`. Imos engadir:
- Un contedor de `data-prepper` empregando a imaxe oficial `opensearchproject/data-prepper:2`. Este contedor conterá dous volumes, un para o directorio de `pipelines` e outro para o ficheiro de configuración `data-prepper-config.yaml`.
```yaml
  data-prepper:
    image: opensearchproject/data-prepper:2
    container_name: data-prepper
    depends_on:
      - opensearch
    ports:
      - "2021:2021"
      - "4900:4900"
    volumes:
      - ./pipelines:/usr/share/data-prepper/pipelines
      - ./data-prepper-config.yaml:/usr/share/data-prepper/config/data-prepper-config.yaml
    networks:
      - opensearch-net
```
- Un contedor de `producer` baseado nunha imaxe `python:3.11-slim` que será o encargado de executar os scripts de python que producián os datos a inxestar en *OpenSearch*.
```yaml
  producer:
    image: python:3.11-slim
    container_name: producer
    depends_on:
      - data-prepper
    working_dir: /app
    volumes:
      - ./producers:/app
      - producer-pip:/pip
    environment:
      - PIP_CACHE_DIR=/pip/cache
      - PYTHONPATH=/pip/site-packages
    command: >
      sh -c "pip install --no-cache-dir --target=/pip/site-packages -r /app/requirements.txt &&
             sleep infinity"
    networks:
      - opensearch-net
```
O ficheiro `docker-compose.yml` completo sería parecido a isto:
```yaml
services:
  opensearch:
    image: opensearchproject/opensearch:2
    container_name: opensearch
    environment:
      - cluster.name=opensearch-cluster
      - node.name=opensearch-node
      - discovery.type=single-node
      - bootstrap.memory_lock=true
      - OPENSEARCH_JAVA_OPTS=-Xms1g -Xmx1g
      - OPENSEARCH_INITIAL_ADMIN_PASSWORD=${OPENSEARCH_INITIAL_ADMIN_PASSWORD}
    ulimits:
      memlock:
        soft: -1
        hard: -1
    volumes:
      - opensearch-data:/usr/share/opensearch/data
    ports:
      - "9200:9200"
      - "9600:9600"
    networks:
      - opensearch-net

  opensearch-dashboards:
    image: opensearchproject/opensearch-dashboards:2
    container_name: opensearch-dashboards
    ports:
      - "5601:5601"
    environment:
      - OPENSEARCH_HOSTS=["https://opensearch:9200"]
    depends_on:
      - opensearch
    networks:
      - opensearch-net

  data-prepper:
    image: opensearchproject/data-prepper:2
    container_name: data-prepper
    depends_on:
      - opensearch
    ports:
      - "2021:2021"
      - "4900:4900"
    volumes:
      - ./pipelines:/usr/share/data-prepper/pipelines
      - ./data-prepper-config.yaml:/usr/share/data-prepper/config/data-prepper-config.yaml
    networks:
      - opensearch-net

  producer:
    image: python:3.11-slim
    container_name: producer
    depends_on:
      - data-prepper
    working_dir: /app
    volumes:
      - ./producers:/app
      - producer-pip:/pip
    environment:
      - PIP_CACHE_DIR=/pip/cache
      - PYTHONPATH=/pip/site-packages
    command: >
      sh -c "pip install --no-cache-dir --target=/pip/site-packages -r /app/requirements.txt &&
             sleep infinity"
    networks:
      - opensearch-net

volumes:
  opensearch-data:
  producer-pip:
networks:
  opensearch-net:
```
> Nota: non é necesario publicar no host o porto 2022, xa que os produtores executan dentro do contedor `producer` e comunícanse con `data-prepper` a través da rede interna de Docker.

---

## 2. Creación manual dos índices con mapping explícito

Antes de iniciar a inxesta de datos, créanse manualmente os índices en OpenSearch definindo explicitamente o *mapping*. 

Isto evita a inferencia automática de tipos e garante:

- coherencia no modelo de datos
- agregacións correctas
- filtros eficientes
- estabilidade a longo prazo

> Se non se define o mapping, OpenSearch decidirá o tipo segundo o primeiro documento recibido.

---

### Índice de logs

No menú **Dev Tools → Console** de OpenSearch Dashboards, executar:

```http
PUT logs_http
{
  "mappings": {
    "properties": {
      "timestamp": { "type": "date" },
      "servizo":   { "type": "keyword" },
      "nivel":     { "type": "keyword" },
      "latency_ms":{ "type": "integer" }
    }
  }
}
```

Decisións de modelado:

- `timestamp` → `date` (permite filtros temporais e `date_histogram`)
- `servizo` → `keyword` (campo categórico)
- `nivel` → `keyword` (info, warn, error)
- `latency_ms` → `integer` (permite agregacións numéricas como `avg`)

---

### Índice de bicicletas

```http
PUT citybikes_http
{
  "mappings": {
    "properties": {
      "timestamp":    { "type": "date" },
      "orixe":        { "type": "keyword" },
      "network_id":   { "type": "keyword" },
      "network_name": { "type": "keyword" },
      "station_id":   { "type": "keyword" },
      "station_name": { "type": "text" },
      "free_bikes":   { "type": "integer" },
      "empty_slots":  { "type": "integer" },
      "latitude":     { "type": "float" },
      "longitude":    { "type": "float" },
      "last_updated": { "type": "date" }
    }
  }
}
```

Decisións de modelado:

- Campos identificadores → `keyword`
- Campos numéricos → `integer` ou `float`
- Datas → `date`
- Nome da estación → `text` (permite busca textual)

---

### Verificación do mapping

Unha vez creados os índices, pódese comprobar o mapping:

```http
GET logs_http/_mapping
```

```http
GET citybikes_http/_mapping
```

Se o mapping é correcto, xa se pode iniciar a inxesta executando os produtores.

---

### Nota importante

Se os produtores xa enviaron datos e o índice foi creado automaticamente, será necesario:

1. Eliminar o índice:
   ```http
   DELETE logs_http
   ```
2. Recrealo co mapping correcto.
3. Reiniciar os produtores.

En contornos reais, este control realízase mediante **index templates**, pero neste laboratorio créanse manualmente para comprender o proceso.

## 3. Crear os scripts produtores
No directorio `producers/` temos dous produtores, un para os logs simulados e outro para datos de bicicletas.
### 3.1 Produtor de logs
ficheiro: `log_producer_http.py`:
```python
import time
import random
import requests
from datetime import datetime, timezone

# Lista de servizos "simulados" para etiquetar cada evento de log.
SERVICES = ["auth", "api", "db", "payments"]

# Niveis posibles do log (informativo, aviso, erro).
LEVELS = ["info", "warn", "error"]

# Endpoint de Data Prepper ao que se enviarán os eventos de logs.
DATAPREPPER_URL = "http://data-prepper:2022/events/logs"

def now_utc_iso():
    """
    Devolve a hora actual en UTC en formato ISO 8601.
    Substitúe '+00:00' por 'Z' para indicar explicitamente UTC.
    Ex.: 2026-02-26T12:34:56.789012Z
    """
    return datetime.now(timezone.utc).isoformat().replace("+00:00", "Z")

# Bucle infinito: xera un evento de log aleatorio e envíao periodicamente a Data Prepper.
while True:
    # Constrúe un evento de log sintético con campos típicos.
    evento = {
        # Timestamp de creación do evento (inxestión no producer).
        "timestamp": now_utc_iso(),

        # Nome do servizo ao que se lle atribúe o log (escollido ao azar).
        "servizo": random.choice(SERVICES),

        # Nivel do log (info/warn/error) escollido ao azar.
        "nivel": random.choice(LEVELS),

        # Latencia simulada (en milisegundos), xerada ao azar entre 10 e 1000.
        "latency_ms": random.randint(10, 1000)
    }

    try:
        # Envía o evento por POST como JSON ao endpoint de Data Prepper.
        # Vaise nunha lista [evento] para manter o formato "batch" (aínda que sexa dun só elemento).
        # Timeout de 5 segundos para evitar quedar bloqueado se o receptor non responde.
        resp = requests.post(DATAPREPPER_URL, json=[evento], timeout=5)

        # Log por consola do status HTTP devolto e do evento que se intentou enviar.
        print("status:", resp.status_code, "| enviado:", evento)
    except Exception as e:
        # Captura erros de rede/timeout, etc., e móstraos por consola.
        print("erro:", e)

    # Agarda 2 segundos antes de xerar e enviar o seguinte evento.
    time.sleep(2)
```

### 3.2 Produtor de datos de bicicletas
```python
import os
import time
import requests
from datetime import datetime, timezone

# ID da rede en CityBikes (p.ex. "bicimad"). Se non existe a variable de contorno,
# úsase "bicimad" por defecto.
CITYBIKES_NETWORK_ID = os.getenv("CITYBIKES_NETWORK_ID", "bicimad")

# URL do endpoint da API de CityBikes para consultar a rede indicada.
CITYBIKES_URL = f"https://api.citybik.es/v2/networks/{CITYBIKES_NETWORK_ID}"

# Endpoint de Data Prepper ao que se enviarán os eventos xerados.
DATAPREPPER_URL = "http://data-prepper:2021/events/bikes"

# Intervalo de consulta en segundos. Por defecto 60s se non se define POLL_SECONDS.
POLL_SECONDS = int(os.getenv("POLL_SECONDS", "60"))

# Timeout HTTP (en segundos) para as peticións GET/POST. Por defecto 10s.
TIMEOUT = int(os.getenv("HTTP_TIMEOUT", "10"))

def now_utc_iso():
    """
    Devolve a hora actual en UTC en formato ISO 8601.
    Converte o sufixo +00:00 a 'Z' para indicar explicitamente UTC.
    Ex.: 2026-02-26T12:34:56.789012Z
    """
    return datetime.now(timezone.utc).isoformat().replace("+00:00", "Z")

# Mensaxe inicial de arranque: indica rede, intervalo de polling e destino de envío.
print(f"[citybikes] network={CITYBIKES_NETWORK_ID} poll={POLL_SECONDS}s -> {DATAPREPPER_URL}")

# Bucle infinito: consulta periódica a CityBikes e envío dos datos a Data Prepper.
while True:
    try:
        # Solicitude GET á API de CityBikes para obter o estado actual da rede.
        r = requests.get(CITYBIKES_URL, timeout=TIMEOUT)

        # Lanza excepción se o status HTTP é de erro (4xx/5xx).
        r.raise_for_status()

        # Parseo da resposta JSON.
        data = r.json()

        # O obxecto raíz trae unha chave "network" cos metadatos da rede e estacións.
        network = data.get("network", {})

        # Lista de estacións dentro da rede. Se non existe, queda como lista baleira.
        stations = network.get("stations", [])

        # Timestamp común (instante de inxestión) para todos os eventos xerados neste ciclo.
        ts = now_utc_iso()

        # Lista onde se acumularán os eventos (un por estación) a enviar a Data Prepper.
        eventos = []

        # Para cada estación da rede, constrúese un evento coas métricas principais.
        for st in stations:
            eventos.append({
                # Timestamp de cando se xera/recopila o evento (inxestión no producer).
                "timestamp": ts,

                # Identifica a orixe dos datos (neste caso, a API citybik.es).
                "orixe": "citybik.es",

                # Identificador da rede consultada (o que se pasou na URL).
                "network_id": CITYBIKES_NETWORK_ID,

                # Nome human-readable da rede (se vén no JSON).
                "network_name": network.get("name"),

                # Identificador da estación (segundo CityBikes).
                "station_id": st.get("id"),

                # Nome da estación.
                "station_name": st.get("name"),

                # Bicis dispoñibles nese momento na estación.
                "free_bikes": st.get("free_bikes"),

                # Ocupación inversa: slots libres para deixar bicis.
                "empty_slots": st.get("empty_slots"),

                # Coordenadas xeográficas da estación.
                "latitude": st.get("latitude"),
                "longitude": st.get("longitude"),

                # Timestamp "last updated" da estación segundo o provedor (pode ser epoch/segundos).
                "last_updated": st.get("timestamp"),
            })

        # Envío por POST de toda a lista de eventos como JSON ao endpoint de Data Prepper.
        resp = requests.post(DATAPREPPER_URL, json=eventos, timeout=TIMEOUT)

        # Log básico: cantos eventos se enviaron e status HTTP do receptor.
        print(f"[citybikes] enviados {len(eventos)} | status={resp.status_code}")

        # Se o status indica redirección/erro (>=300), amósase un anaco do corpo da resposta.
        if resp.status_code >= 300:
            print(resp.text[:200])

    except Exception as e:
        # Captura calquera excepción (rede, JSON, status HTTP, etc.) e rexístrase por consola.
        print("[citybikes] erro:", repr(e))

    # Agarda o intervalo configurado antes de repetir o ciclo.
    time.sleep(POLL_SECONDS)
```

## 4. Crear e validar as pipelines

No directorio `pipelines/` temos dúas pipelines: unha para logs simulados e outra para datos de bicicletas.

### 4.1 Pipeline de logs

Ficheiro: `logs-to-opensearch.yml`

```yaml
logs-to-opensearch:
  source:
    http:
      path: "/events/logs"
      port: 2022

  sink:
    - opensearch:
        hosts: [ "https://opensearch:9200" ]
        insecure: true
        username: "admin"
        password: "Opensearch#2025"
        index: "logs_http"
```

Esta pipeline:

- Expón un endpoint HTTP en `2022/events/logs`
- Recibe eventos en formato JSON
- Indexa os datos no índice `logs_http`

> É importante que o porto coincida co definido no script `log_producer_http.py`.

---

### 4.2 Pipeline de bicicletas

Ficheiro: `bikes-to-opensearch.yml`

```yaml
bikes-to-opensearch:
  source:
    http:
      path: "/events/bikes"
      port: 2021

  sink:
    - opensearch:
        hosts: [ "https://opensearch:9200" ]
        insecure: true
        username: "admin"
        password: "Opensearch#2025"
        index: "citybikes_http"
```

Esta pipeline:

- Expón o endpoint `2021/events/bikes`
- Recibe datos da API pública de citybik.es
- Indexa no índice `citybikes_http`

---

## 5. Arrincar o stack

Desde o directorio do proxecto:

```bash
docker compose up -d
```

Comprobar que todos os contedores están activos:

```bash
docker ps
```

Deberían aparecer:

- opensearch
- opensearch-dashboards
- data-prepper
- producer

> Podemos asegurarnos de que está todo listo se esperamos a que a interfaz de opensearch-dashboards está activa.
Comprobar logs de Data Prepper:

```bash
docker logs -f data-prepper
```

Se as pipelines cargan correctamente, aparecerán mensaxes indicando que os endpoints HTTP están activos.

---

## 6. Executar os produtores

Acceder ao contedor producer:

```bash
docker exec -it producer sh
```

### 6.1 Executar produtor de logs

```bash
python log_producer_http.py
```

Cada 2 segundos enviará un evento como:

```json
{
  "timestamp": "...",
  "servizo": "api",
  "nivel": "info",
  "latency_ms": 345
}
```

---

### 6.2 Executar produtor de bicicletas

Nun segundo terminal:

```bash
docker exec -it producer sh
python bike_producer_http.py
```

Cada 60 segundos consultará a API de citybik.es e enviará os datos a Data Prepper.

---

## 7. Verificar en OpenSearch

Pódese comprobar directamente mediante API:

```bash
curl -k -u admin:Opensearch#2025 https://localhost:9200/_cat/indices?v
```

Deberían aparecer:

- logs_http
- citybikes_http

Comprobar número de documentos:

```bash
curl -k -u admin:Opensearch#2025 https://localhost:9200/logs_http/_count
```

---

## 8. Visualización en OpenSearch Dashboards

1. Acceder a http://localhost:5601
2. Ir a **Management** -> **Dashboard Management** -> **Index Patterns** e comprobar que existen os índices.
3. Crear os **Index Pattern**'s:
   - `logs_http`
   - `citybikes_http`
4. Seleccionar o campo `timestamp` como campo temporal.
5. Ir a Discover para visualizar eventos en tempo real.

---

## 9. Experimentos propostos

### 9.1 Engadir un processor

Modificar a pipeline de logs para engadir un campo:

```yaml
processor:
  - add_entries:
      entries:
        - key: "entorno"
          value: "laboratorio"
```

Reiniciar Data Prepper:

```bash
docker compose restart data-prepper
```

Comprobar que o novo campo aparece nos documentos indexados.

---

### 9.2 Filtrar só erros

Engadir un processor `drop_events` para descartar eventos que non sexan erro.

---

### 9.3 Crear múltiples sinks

Enviar simultaneamente a:

- OpenSearch
- stdout

Para observar os eventos tamén nos logs do contedor.

---

## 10. Conclusión do laboratorio

Neste laboratorio construíuse unha arquitectura básica de inxestión:

```
Producer → Data Prepper → OpenSearch → Dashboards
```

Aprendeuse a:

- Configurar pipelines HTTP.
- Despregar Data Prepper en Docker.
- Integrar produtores en Python.
- Verificar indexación en OpenSearch.
- Visualizar datos en Dashboards.

Este patrón é a base de arquitecturas de observabilidade e inxestión en contornos reais.